FORMAT REQUIREMENT — COPY-PASTABLE MARKDOWN

Give me the response as a complete, standalone Markdown (.md) file.

IMPORTANT:
- I want to copy the entire response and paste it directly into a `.md` file.
- Do NOT render the Markdown headings.
- I need to SEE the actual Markdown syntax such as `#`, `##`, `###`, `>`, `-`, `|`, and code fences.
- Therefore, wrap the ENTIRE response inside ONE outer fenced code block using four backticks:

````markdown
# Main Heading

## Section

Content here.

```java
// Code example
